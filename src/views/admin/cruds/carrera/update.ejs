<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title %></title>
        <%- include('../../../partials/head') %>
    </head>
    <body class="bg-light">
        <%- include('../../../partials/nav_admin') %>

        <main class="container p-5">
            <h2 class="text-secondary mb-4"><%= title %></h2>

            <!-- Error Alert -->
            <% if (error) { %>
            <div
                class="alert alert-danger alert-dismissible fade show mb-4"
                role="alert"
            >
                <strong>Error!</strong> <%= error.message %>
                <button
                    type="button"
                    class="btn-close"
                    data-bs-dismiss="alert"
                    aria-label="Close"
                ></button>
            </div>
            <% } %>

            <!-- Formulario -->
            <form
                class="card border-0 shadow-lg rounded-4 p-4"
                method="post"
                action="/admin/carrera/update/:<%= carrera.carrera_id %>"
            >
                <div class="row mb-4">
                    <div class="col-md-6">
                        <label for="carr_clave" class="form-label">Clave</label>
                        <input
                            name="carr_clave"
                            id="carr_clave"
                            type="text"
                            class="form-control border-secondary shadow-sm"
                            value="<%= carrera.carrera_clave %>"
                            required
                        />
                    </div>
                    <div class="col-md-6">
                        <label for="carr_nombre" class="form-label">Nombre</label>
                        <input
                            name="carr_nombre"
                            id="carr_nombre"
                            type="text"
                            class="form-control border-secondary shadow-sm"
                            value="<%= carrera.carrera_nombre %>"
                            required
                        />
                    </div>
                </div>

                <!-- Campo oculto -->
                <input
                    type="hidden"
                    name="carr_id"
                    value="<%= carrera.carrera_id %>"
                />

                <div class="d-grid">
                    <button type="submit" class="btn btn-secondary shadow-sm">
                        Actualizar Carrera
                    </button>
                </div>
            </form>
        </main>

        <%- include('../../../partials/footer') %>
    </body>
</html>
