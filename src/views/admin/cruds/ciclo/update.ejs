<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title %></title>
        <%- include('../../../partials/head') %>
    </head>
    <body class="bg-light">
        <%- include('../../../partials/nav_admin') %>

        <main class="container p-5">
            <div class="d-flex justify-content-center">
                <div
                    class="card border-0 shadow-lg rounded-4"
                    style="max-width: 600px"
                >
                    <div class="card-body p-5">
                        <h2 class="text-center text-secondary mb-4">
                            <%= title %>
                        </h2>

                        <% if (error) { %>
                        <div
                            class="alert alert-danger alert-dismissible fade show mb-4"
                            role="alert"
                        >
                            <strong>Error!</strong> <%= error.message %>
                            <button
                                type="button"
                                class="btn-close"
                                data-bs-dismiss="alert"
                                aria-label="Close"
                            ></button>
                        </div>
                        <% } %>

                        <form
                            method="post"
                            action="/admin/ciclo/update/<%= ciclo.ciclo_id %>"
                        >
                            <div class="mb-4">
                                <label for="ciclo_nombre" class="form-label"
                                    >Nombre</label
                                >
                                <input
                                    name="ciclo_nombre"
                                    id="ciclo_nombre"
                                    type="text"
                                    class="form-control border-secondary shadow-sm"
                                    value="<%= ciclo.ciclo_nombre %>"
                                    required
                                />
                            </div>

                            <div class="mb-4">
                                <label for="ciclo_inicio" class="form-label"
                                    >Fecha de Inicio</label
                                >
                                <input
                                    name="ciclo_inicio"
                                    id="ciclo_inicio"
                                    type="date"
                                    class="form-control border-secondary shadow-sm"
                                    value="<%= ciclo.ciclo_inicio %>"
                                    required
                                />
                            </div>

                            <div class="mb-4">
                                <label for="ciclo_final" class="form-label"
                                    >Fecha de Finalización</label
                                >
                                <input
                                    name="ciclo_final"
                                    id="ciclo_final"
                                    type="date"
                                    class="form-control border-secondary shadow-sm"
                                    value="<%= ciclo.ciclo_final %>"
                                    required
                                />
                            </div>

                            <div class="mb-4 form-check">
                                <input type="checkbox" class="form-check-input
                                border-secondary shadow-sm" id="ciclo_activo"
                                name="ciclo_activo" <%= ciclo.ciclo_activo ==
                                "SI" ? "checked" : "" %> />
                                <label
                                    for="ciclo_activo"
                                    class="form-check-label"
                                    >Activo</label
                                >
                            </div>

                            <input
                                type="hidden"
                                name="ciclo_id"
                                value="<%= ciclo.ciclo_id %>"
                            />

                            <div class="d-grid">
                                <button
                                    type="submit"
                                    class="btn btn-secondary shadow-sm"
                                >
                                    Actualizar
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </main>

        <%- include('../../../partials/footer') %>
    </body>
</html>
