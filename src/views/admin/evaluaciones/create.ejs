<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title %></title>
        <%- include('../../partials/head') %>
    </head>
    <body>
        <%- include('../../partials/nav_admin') %>

        <main class="container py-5">
            <!-- Título Principal -->
            <div class="text-center mb-4">
                <h1 class="text-secondary">
                    <i class="bi bi-calendar2-date"></i> Crear Evaluaciones
                </h1>
                <p class="text-muted">
                    Seleccione un ciclo escolar para gestionar las evaluaciones.
                </p>
            </div>

            <!-- Formulario dentro de una Card -->
            <div class="card shadow-lg">
                <div class="card-header bg-secondary text-white">
                    <h4 class="mb-0 text-center">
                        <i class="fas fa-calendar-alt"></i> Seleccionar Ciclo
                        Escolar
                    </h4>
                </div>
                <div class="card-body">
                    <form
                        action="/admin/evaluaciones/create"
                        method="post"
                        class="needs-validation"
                        novalidate
                    >
                        <!-- Selección del Ciclo -->
                        <div class="mb-4">
                            <label for="ciclo_id" class="form-label"
                                ><i class="fas fa-list-ul"></i> Ciclo
                                Escolar</label
                            >
                            <select
                                class="form-select"
                                name="ciclo_id"
                                id="ciclo_id"
                                required
                            >
                                <option value="" disabled selected>
                                    Seleccione una opción
                                </option>
                                <% ciclos.forEach(ciclo => { %>
                                <option value="<%= ciclo.ciclo_id %>">
                                    <i class="fas fa-calendar"></i> <%=
                                    ciclo.ciclo_nombre %>
                                </option>
                                <% }) %>
                            </select>
                            <div class="invalid-feedback">
                                Por favor, seleccione un ciclo escolar válido.
                            </div>
                        </div>

                        <!-- Botón de Envío -->
                        <div class="d-flex justify-content-center">
                            <button
                                type="submit"
                                class="btn btn-secondary btn-lg px-5"
                            >
                                <i class="fas fa-check-circle"></i> Crear
                                Evaluaciones
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </main>

        <%- include('../../partials/footer') %>
    </body>
</html>
